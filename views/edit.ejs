<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Song</title>
    <style>
      body {
          font-family: Arial, sans-serif;
          background-color: #530741; 
          display: flex;
          justify-content: center;
          align-items: center;
          height: 100vh;
          margin: 0;
      }
  
      h1 {
          color: #6c5ce7; 
          text-align: center;
          font-size: 24px;
          font-weight: bold;
          margin-bottom: 20px;
      }
  
      form {
          max-width: 100%; 
          margin: 40px auto;
          padding: 80px;
          background-color: #3d3d5c; 
          border: none;
          border-radius: 10px;
          box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
          display: flex;
          flex-direction: column;
          align-items: center;
      }
  
      label {
          display: block;
          margin-bottom: 10px;
          color: #ccc;
          padding-top: 5px;
          width: 100%;
      }
  
      input[type="text"], textarea {
          width: 95%;
          padding: 10px;
          margin-bottom: 20px;
          border: none;
          border-radius: 5px;
          background-color: #4a4a4d;
          color: white;
      }
  
      input[type="file"] {
          margin-bottom: 20px;
          border: none;
          border-radius: 5px;
          height: 20px;
          background-color: #4a4a4d;
          color: white;
      }
  
      .button-container {
          display: flex;
          justify-content: space-between;
          width: 100%;
      }
  
      button[type="submit"] {
          background-color: #34c759; 
          color: #fff;
          padding: 10px 20px;
          border: none;
          border-radius: 5px;
          cursor: pointer;
      }
  
      button[type="submit"]:hover {
          background-color: #2e865f; 
      }
  
      .back-link {
          text-decoration: none;
          color: #ff3b30; 
          padding: 10px 20px;
          border-radius: 5px;
          background-color: transparent;
          border: 1px solid #ff3b30;
          display: inline-block;
          text-align: center;
      }
  
      .back-link:hover {
          color: #e62e2e; 
          border-color: #e62e2e;
      }
  </style>
  
</head>
<body>
    <form action="/edit/<%= song.id %>" method="post" enctype="multipart/form-data">
      <h1 style="text-align: center; font-size: 24px; font-weight: bold; color: #e91e63;">EDIT MUSIC</h1>

      <label for="title">Title</label>
      <input type="text" name="title" value="<%= song.title %>" required>

      <label for="artist">Artist</label>
      <input type="text" name="artist" value="<%= song.artist %>" required>

      <label for="lyrics">Lyrics</label>
      <textarea name="lyrics" required><%= song.lyrics %></textarea>

      <label for="music">Music</label>
      <input type="file" name="musicFile">
      <input type="hidden" name="oldMusic" value="<%= song.music %>">

      <label for="image">Image</label>
      <input type="file" name="imageFile">
      <input type="hidden" name="oldImage" value="<%= song.image %>">

      <button type="submit">Save Changes</button>
      <a href="/" class="back-link">Cancel</a>
    </form>
</body>
</html>
